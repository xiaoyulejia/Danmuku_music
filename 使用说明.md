# Damuku_music 使用说明

本文按“安装 → 生成链接 → 配置音乐 → 接入 OBS → 调试排障”的顺序说明当前版本的使用方法。

## 1. 安装和启动

### 1.1 环境要求

- Node.js 18 或更高版本，推荐 Node.js 24 LTS；
- npm；
- Windows 一键启动不需要手动安装 PM2，项目依赖会提供；
- 如果使用 QQ 音乐，需要另外准备兼容 `http://localhost:3300` 的 QQMusicApi 服务。

### 1.2 Windows 一键启动

双击项目根目录的：

```text
启动点歌台.bat
```

首次运行会安装依赖。脚本优先使用国内 npm 镜像，失败后自动切换 npm 官方源，不会永久修改全局 npm 源。

启动后会打开：

```text
http://localhost:8000/order/launcher.html
```

如果没有自动打开，复制到浏览器访问即可。

### 1.3 命令行启动

```bash
npm install
npm run dev       # 当前终端前台运行
npm run launch    # 启动并打开启动器
npm start         # PM2 后台运行
npm run stop
npm run restart
npm run log
npm test
```

两种运行方式的区别：

- `npm run launch` 适合本地临时使用，关闭启动窗口会停止本次启动的服务；
- `npm start` 适合长期运行，服务交给 PM2 管理，使用 `npm run stop` 停止。

## 2. 启动器和页面分工

在启动器填写 B 站直播间号，只填写数字，例如：

```text
4646297
```

点击“生成链接”后会生成三个页面：

| 页面 | 示例 | 作用 |
| --- | --- | --- |
| OBS 播放页面 | `/order/?roomid=4646297&source=obs` | 唯一实际播放音频、接收弹幕、维护队列 |
| 控制页面 | `/order/?roomid=4646297&livemode=false&source=control` | 普通浏览器控制歌曲、队列、音量、歌单和设置 |
| 透明歌词页面 | `/order/?roomid=4646297&source=obs&lyric=true` | 只显示歌词，适合作为独立浏览器源 |

启动器中的“生成多场景链接”可以选择 1～3 个场景，默认选择 1 个。生成后会额外提供 `scene-a`、`scene-b`、`scene-c` 对应的复制/打开按钮；使用前需要在设置页启用多场景交接。启动器底部也会显示产品版本和前端构建号。

产品版本和构建号统一维护在 `config/version.js`，其中 `productVersion` 填写 DEV/STA 产品版本，`buildId` 填写前后端共用的构建号。修改后重启服务，浏览器页面会自动读取新配置。

还会提供设置页：

```text
/order/settings.html?roomid=4646297
```

推荐先打开 OBS 播放页面，再打开控制页面。服务端会按房间保存歌曲、队列、空闲歌单和播放状态；即使 OBS 和控制页不在同一个浏览器中，也可以同步。

### 直播姬多场景交接

直播姬最稳定的用法是只创建一个 `source=obs` 浏览器源，然后在其他场景选择“添加现有来源”。该来源属性中关闭“源不可见时关闭”和“场景激活时刷新浏览器源”，避免切场景时重建网页。

如果直播姬不能引用同一个来源，才为每个场景使用独立地址，并加入 `handoff=scene` 与唯一的 `instance`：

```text
http://127.0.0.1:8000/order/?roomid=4646297&source=obs&handoff=scene&instance=scene-a
http://127.0.0.1:8000/order/?roomid=4646297&source=obs&handoff=scene&instance=scene-b
```

独立 WebView 模式下可以开启“源不可见时关闭”，但应关闭“场景激活时刷新浏览器源”。然后在设置页勾选“启用直播姬多场景手动切换”，保存并刷新两个直播姬页面及控制页。切换场景后，在控制页的“播放源切换”面板选择目标 `instance` 并点击切换；旧页面会被 fencing，新页面恢复歌曲和进度。每个场景的 `instance` 必须固定且不能重复。OBS 使用“添加现有来源”时不要开启此选项。

直播姬和点歌服务在同一台电脑时使用 `127.0.0.1`；如果服务运行在另一台电脑，需将服务监听地址改为 `0.0.0.0`，并在直播姬中填写启动日志显示的局域网 IPv4 地址。

### 2.1 OBS 页面

推荐地址：

```text
http://127.0.0.1:8000/order/?roomid=4646297&source=obs
```

在 OBS 中添加“浏览器”来源，建议先使用约 `420 × 300～600` 的尺寸，再按直播画面调整。页面默认透明，通常只显示点歌队列。

OBS 页面承担：

- 连接 B 站弹幕；
- 处理观众点歌；
- 加载空闲歌单；
- 播放实际音频；
- 向服务端发布当前播放状态。

### 2.2 控制页面

推荐地址：

```text
http://127.0.0.1:8000/order/?roomid=4646297&livemode=false&source=control
```

控制页是镜像页，不会再次连接弹幕、不加载第二份空闲歌单，也不会播放第二份音频。它可以向 OBS 播放页发送：

- 播放、暂停、切歌；
- 启用声音；
- 音量调整；
- 队列上移、删除、排序；
- 加载或切换空闲歌单；
- 进度跳转（需要设置页开启“允许控制页跳转”）。

如果已经有 OBS 播放端，重复打开 `source=obs` 链接也会自动成为镜像页，不会生成第二套播放状态。

### 2.3 透明歌词页面

启动器生成：

```text
http://127.0.0.1:8000/order/?roomid=4646297&source=obs&lyric=true
```

它只订阅当前房间状态并显示歌词，不负责播放。适合在 OBS 中作为单独的透明浏览器源使用。歌词页显示哪些内容由设置页的歌词选项控制。

### 2.4 设置页面

```text
http://127.0.0.1:8000/order/settings.html?roomid=4646297
```

设置页不会自动播放歌曲。页面会把 `settings=1` 加到内部嵌入地址中，因此一般不需要手动拼接 `settings=1`。

设置分为：音乐登录、点歌设置、弹幕设置、显示设置和队列管理。

## 3. URL 参数完整说明

主页面格式：

```text
http://主机:端口/order/?roomid=房间号&参数=值
```

参数必须用 `&` 连接：

```text
正确：?roomid=4646297&livemode=false&source=control
```

当前代码为了兼容旧链接，也会把后续的 `?` 当成 `&` 处理，但不建议依赖这个兼容行为。

### 3.1 房间参数

#### `roomid`

B 站直播间号，正整数，主页面必填：

```text
?roomid=4646297
```

可以填写 B 站短房间号，服务端会自动解析真实房间号。不要填写直播间 URL、带字母的房间名或空值。

#### `room_id`

`roomid` 的兼容别名，页面和后端调试 API 都支持：

```text
?room_id=4646297
```

同一个地址不要同时填写两个不同的房间号；如果同时存在，前端通常优先读取 `roomid`。

### 3.2 页面模式参数

#### `source`

指定页面角色：

| 值 | 作用 |
| --- | --- |
| `obs` | 推荐的 OBS 播放源标记 |
| `control` | 控制/镜像页 |
| `monitor` | 监控/镜像页 |
| `preview` | 预览/镜像页 |
| 不填或其他值 | 当 `livemode` 为真时按直播布局处理 |

推荐直接使用启动器生成的 `source=obs` 和 `source=control`，不要省略。

#### `livemode`

控制页面是否使用直播布局：

```text
直播模式：?roomid=4646297&livemode=true
控制模式：?roomid=4646297&livemode=false
```

以下值表示关闭：`0`、`false`、`no`、`off`；其他值（包括不填）按开启处理。

`livemode=false` 会成为镜像页，不会创建第二个音频播放器。`source=control`、`source=monitor`、`source=preview` 也会使页面按镜像/控制角色运行。

#### `lyric`

开启透明歌词模式：

```text
?roomid=4646297&lyric=true
```

值为 `1`、`true`、`yes`、`on` 时生效。歌词模式会强制关闭直播播放逻辑，即使同时写了 `livemode=true`，也不会成为实际播放端。

#### `settings`

内部设置页标志：

```text
?roomid=4646297&settings=1
```

它由 `settings.html` 自动添加，用于把主页面变成全屏设置页。直接访问 `settings.html` 更方便。

### 3.3 弹幕连接参数

#### `realtime`

开启实时 WebSocket 模式：

```text
?roomid=4646297&realtime=1
```

值为 `1`、`true`、`yes`、`on` 时生效。不填写时使用默认的历史弹幕轮询模式。

实时模式的连接由 Node 服务端统一维护，浏览器不会接触上游 Token、Cookie、真实 host 或 ACK 参数。

#### `history`

强制使用历史弹幕轮询：

```text
?roomid=4646297&realtime=1&history=1
```

值为 `1`、`true`、`yes`、`on` 时生效。它的优先级高于 `realtime=1`，因此上面的组合仍然是历史模式。通常无需填写该参数；想明确指定稳定模式时可以使用：

```text
?roomid=4646297&history=1
```

历史模式每 3 秒读取最近弹幕并建立基线，页面首次加载时的旧弹幕不会触发点歌。

#### `debug`

开启浏览器调试日志：

```text
?roomid=4646297&realtime=1&debug=1
```

值为 `1`、`true`、`yes`、`on` 时生效。需要打开浏览器开发者工具 → Console 才能看到完整信息。

控制/镜像页带上 `debug=1` 时也会建立只读弹幕观察连接；未填写 `realtime=1` 时观察默认的历史弹幕轮询，填写后观察实时 WebSocket，均不会触发点歌：

```text
?roomid=4646297&livemode=false&source=control&debug=1
```

#### `uid`

指定管理员 B 站 UID：

```text
?roomid=4646297&uid=106307213
```

该 UID 用于识别弹幕指令中的管理员。不要把昵称、房间号或个人主页 URL 填到 `uid`。如果不填，默认管理员 UID 为 `0`，暂停和播放通常不会被普通用户触发；建议 OBS 播放页和控制页使用同一个 `uid`。

## 4. 音乐平台设置

### 4.1 网易云音乐

1. 打开设置页；
2. 在“音乐平台”选择网易云音乐；
3. 点击“刷新二维码”；
4. 使用网易云音乐 App 扫码登录；
5. 点击“刷新状态”确认登录；
6. 填写空闲歌单 ID，点击“加载歌单”。

网易云 Cookie 会保存在本机浏览器和本地服务凭据文件中，用于搜索、获取播放地址、歌词和歌单。不要把 Cookie 复制到 README、日志或 Git。

### 4.2 QQ 音乐

1. 确认 QQMusicApi 服务已运行在 `http://localhost:3300`；
2. 设置页选择 QQ 音乐；
3. 填写 QQ 号和 Cookie；
4. 点击“设置 Cookie”。

QQ 音乐当前支持弹幕搜索和歌曲播放接口，`getSongList()` 未实现，因此不能像网易云一样加载 QQ 空闲歌单。QQ API 地址可以在 `config/webapi.js` 修改为其他服务地址。

### 4.3 点歌平台前缀

```text
点歌起风了       # 使用当前默认音乐平台
点歌wy起风了     # 强制使用网易云音乐
点歌qq起风了     # 强制使用 QQ 音乐
```

当前默认音乐平台一般是网易云音乐。搜索结果取对应平台的第一首匹配歌曲。

## 5. 点歌和管理员指令

### 5.1 点歌

```text
点歌歌曲名
点歌wy歌曲名
点歌qq歌曲名
```

点歌成功后进入房间队列，服务端统一决定队列顺序和当前播放状态。

### 5.2 切歌

```text
切歌
```

允许条件：

- 当前播放的是空闲歌单歌曲；
- 发送者是管理员 UID；
- 发送者是当前歌曲的点歌人。

其他观众会收到“不能切别人点的歌”的提示。

### 5.3 暂停和播放

```text
暂停
播放
```

这两个指令只允许管理员 UID 执行。浏览器自动播放策略导致没有声音时，也可以直接在 OBS 播放页点击“启用声音”。

## 6. 设置项说明

### 6.1 点歌限制

| 设置 | 含义 | 单位/范围 |
| --- | --- | --- |
| 用户点歌数 | 单个用户最多保留的点歌数量 | 默认 3，最小 1 |
| 最大点歌数 | 全房间最多保留的点歌数量 | 默认 15，最小 1 |
| 最大歌曲时长 | 超过该时长的歌曲不能点 | 秒，`0` 表示不限制 |
| 超时限播时长 | 播放到该秒数后自动下一首 | 秒，`0` 表示不限制 |

历史用户和历史歌曲最多保留最近 50 条；服务端设置文件会对数组做有限长度保护。

### 6.2 显示设置

- 主题：深色或浅色；
- 播放层透明度：20～100；
- 背景模糊：0～50 像素；
- 卡片背景颜色；
- 当前歌曲名称、歌手和队列表头/内容的字号与颜色；框体缩放时会按比例同步字号（窄框体保留最小可读比例）；
- 显示歌词、翻译；
- 歌词显示模式：换行或横向滚动；
- 歌词时间偏移：-5000～5000 毫秒；
- 歌词字号：12～64；
- 歌词中英文字体：可分别手动输入字体名，也可读取系统字体列表后从下拉选项选择；
- 歌词颜色和透明度；
- 重置颜色配置：一键恢复背景、歌曲、歌手、队列和歌词颜色默认值；
- 歌词页显示前后歌词行数：0～3；
- 歌词页宽度：50%～100%；
- 是否允许控制页拖动进度；
- 直播模式是否显示当前歌曲卡片、控制按钮、队列标题、点歌人和提示消息；
- 直播模式队列简洁模式：隐藏队列外框和标题，仅显示歌曲、歌手、点歌人表格；
- 自定义 CSS。

显示设置是全局设置；点歌限制和音乐登录主要按房间保存。设置修改后会通过服务端同步到 OBS、控制页和歌词页。

自定义 CSS 示例：

```css
.playerCard,
.queueCard {
    border-radius: 8px;
}

.playerActions button {
    font-size: 16px;
}

.orderTable th:nth-child(3),
.orderTable td:nth-child(3) {
    display: none;
}
```

常用选择器：`.playerCard`、`.queueCard`、`.playerActions`、`.orderTable`、`.alertBox .text`。

## 7. B 站弹幕连接说明

项目使用 B 站普通直播弹幕协议，不依赖直播开放平台互动玩法许可，也不要求主播或观众登录 B 站。

默认历史模式流程：

1. 访问历史弹幕接口；
2. 以首次结果建立去重基线；
3. 每 3 秒读取最近弹幕；
4. 只把新增弹幕交给点歌解析器。

实时模式流程：

1. Node 获取并缓存匿名会话、`buvid`、Token 和 host 列表；
2. 按真实房间号建立共享上游 WebSocket；
3. 处理 Brotli/Zlib、半包、多内层包和 ACK；
4. 将标准化弹幕转发给浏览器订阅者；
5. 短断线时用历史接口补偿并去重。

实时模式地址：

```text
http://127.0.0.1:8000/order/?roomid=4646297&source=obs&realtime=1
```

不要在 URL 中添加 B 站 Cookie、Token、`host`、`buvid` 或其他上游连接参数。

## 8. 调试方法

### 8.1 页面 Console

建议先用下面的地址：

```text
http://127.0.0.1:8000/order/?roomid=4646297&source=obs&realtime=1&debug=1
```

Console 中可看到：

- 房间号、历史/实时模式和播放端租约；
- WebSocket 连接、认证、重连和实时弹幕；
- 历史弹幕基线和新增弹幕；
- 网易云搜索、播放地址、歌词和歌单请求；
- 音频加载、元数据、播放、暂停、等待、错误；
- 控制页发出的同步命令和 HTTP 响应。

日志会尽量隐藏 Cookie、Token 和完整播放地址参数，但仍不要把完整 Console 导出公开。

### 8.2 后端健康接口

默认 API 前缀为 `/order/bili-api`：

```text
GET /order/bili-api/live/health?room_id=4646297
GET /order/bili-api/live/danmu-info?room_id=4646297
GET /order/bili-api/live/danmu-history?room_id=4646297
GET /order/bili-api/live/metrics?room_id=4646297
GET /order/bili-api/live/sync-state?room_id=4646297
```

浏览器直接访问示例：

```text
http://127.0.0.1:8000/order/bili-api/live/health?room_id=4646297
```

接口说明：

- `health`：确认 Node 服务是否运行，并返回后端构建版本；
- `danmu-info`：检查房间解析、弹幕 Token 和 host；
- `danmu-history`：查看 B 站历史弹幕原始结果，适合检查房间号和字段；
- `metrics`：查看实时上游、解析和浏览器转发计数；
- `sync-state`：查看房间当前歌曲、队列、音量、播放端和状态。

`metrics` 只有在该房间当前存在实时连接时才有数据。历史模式没有实时上游指标。

### 8.3 房间同步接口参数

控制页和 OBS 使用的同步接口也可以手动检查：

```text
GET /order/bili-api/live/sync-state?room_id=4646297
GET /order/bili-api/live/sync-commands?room_id=4646297&after=0&since=0
```

参数说明：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `room_id` | 字符串/数字 | 房间标识，也支持兼容别名 `roomid` |
| `after` | 非负整数 | 只返回 `sequence` 大于该值的控制命令，默认 `0` |
| `since` | Unix 毫秒时间戳 | 只返回 `createdAt` 大于等于该值的命令，默认 `0` |

例如只查看最近 5 分钟的命令：

```text
http://127.0.0.1:8000/order/bili-api/live/sync-commands?room_id=4646297&after=0&since=当前时间戳减300000
```

`sync-state` 是 GET；`sync-command`、`sync-state` 发布和 `sync-claim` 是内部 POST 接口，正常使用不需要手动调用。接口返回的状态文件和命令日志可能包含当前歌曲、队列和用户显示名，不要直接公开调试结果。

### 8.4 实时指标怎么判断

打开：

```text
http://127.0.0.1:8000/order/bili-api/live/metrics?room_id=4646297
```

重点检查：

| 字段 | 判断方法 |
| --- | --- |
| `commandCountByType.DANMU_MSG` | B 站上游是否真的下发普通聊天弹幕 |
| `danmuDecodedCount` | Node 是否成功解析普通弹幕 |
| `proxySentCount` | Node 是否向本地订阅者发送 |
| `subscriberDeliveryCount` | 浏览器订阅者是否收到 |
| `parseErrorCount` | 理想情况下应保持 0 |
| `remainderBytes` | 不应持续增长，持续增长表示可能有半包或协议解析问题 |
| `httpAckFailureCount` | 不应持续增长 |

定位思路：

- `DANMU_MSG=0`：优先检查房间是否开播、B 站上游认证和实时连接；
- 上游有 `DANMU_MSG`、`danmuDecodedCount=0`：检查解析或协议兼容；
- `danmuDecodedCount` 增长、`subscriberDeliveryCount=0`：检查浏览器 WebSocket 或页面版本；
- 页面有弹幕但没有点歌：检查弹幕内容是否以“点歌”开头、音乐 API 是否可用、黑名单和数量限制。

### 8.5 服务端日志和运行数据

```text
logs/out.log
logs/error.log
cache/order-sync/
data/settings/
data/history/
data/credentials/
```

直接 `npm run dev` 时日志在当前终端；PM2 模式执行：

```bash
npm run log
```

## 9. 常见问题

### 9.1 页面提示后端服务未运行

确认：

1. 启动窗口没有报错；
2. 端口与 URL 一致；
3. 浏览器访问 `http://127.0.0.1:8000/order/launcher.html`；
4. 访问 `live/health` 是否返回 JSON；
5. 端口被旧进程占用时，先关闭旧服务，再重新运行启动器。

### 9.2 修改代码后页面没有更新

- 修改 Node 文件：重启 `npm run dev` 或执行 `npm run restart`；
- 修改前端文件：Ctrl+F5；
- 页面提示前后端构建版本不一致：点击“清空缓存并刷新”；
- 直播姬：删除旧浏览器源后重新添加，避免内置 WebView 使用旧脚本。

### 9.3 OBS 没有声音

浏览器通常会阻止自动播放。打开 OBS 播放页一次并点击“启用声音”，然后检查：

- OBS 浏览器源是否启用音频；
- OBS 音频监视器和输出设备；
- 系统音量；
- 网易云/QQ 播放地址是否可获取。

控制页的音量滑块只发送命令，实际音频仍由 OBS 播放页输出。

### 9.4 OBS 和控制页状态不一致

确认两边：

- `roomid` 完全相同；
- OBS 使用 `source=obs`；
- 控制页使用 `livemode=false&source=control`；
- OBS 页面先打开并保持在线；
- 服务端没有运行旧版本；
- 直播姬使用了启动器生成的 `127.0.0.1` 地址。

如仍不一致，访问：

```text
http://127.0.0.1:8000/order/bili-api/live/sync-state?room_id=4646297
```

确认服务端的 `currentSong`、`queue`、`publisherId` 和 `publisherHeartbeatAt`。

### 9.5 没有收到弹幕

先确认直播间正在开播。分别测试：

```text
# 默认历史模式
http://127.0.0.1:8000/order/?roomid=4646297&source=obs&debug=1

# 实时模式
http://127.0.0.1:8000/order/?roomid=4646297&source=obs&realtime=1&debug=1
```

实时模式查看 Console 是否出现 `upstream authenticated`，再访问 `live/metrics`。注意认证成功只代表上游接受连接，不等于普通聊天弹幕已经下发。

历史模式首次加载会建立旧弹幕基线，页面刚打开前已经存在的弹幕不会触发点歌；请在页面打开后重新发送一条测试弹幕。

### 9.6 收到弹幕但点歌失败

按顺序检查：

1. 弹幕是否严格以 `点歌` 开头；
2. 是否误把平台写成了 `网易云`、`ncm` 等不支持的前缀，当前只识别 `wy` 和 `qq`；
3. 音乐平台 API 是否可访问；
4. 网易云是否登录；
5. QQMusicApi 是否运行在 `localhost:3300`；
6. 歌曲是否命中歌曲黑名单；
7. 用户或全局队列是否已达到上限；
8. 歌曲时长是否超过限制。

### 9.7 QQ 音乐不可用

检查 `config/webapi.js`：

```js
qqmusic_api: "http://localhost:3300"
```

然后确认该地址的 QQMusicApi 服务存在并能响应 `/search` 和 `/song/url`。当前项目没有 QQ 空闲歌单实现，因此 QQ 平台不要依赖空闲歌单功能。

### 9.8 直播姬无法访问或无法同步

直播姬使用独立 WebView，不共享 Chrome 的 localStorage。请：

1. 使用启动器生成的 `127.0.0.1` 地址；
2. 直播姬中放 OBS 播放链接，不要放控制链接；
3. 删除旧浏览器源后重新添加；
4. 如果仍无法访问，将 `config/config.yaml` 中的 `web_server_host` 改为 `0.0.0.0`，重启服务；
5. 使用启动日志中的局域网 IPv4 地址，例如 `http://192.168.1.100:8000/order/?roomid=4646297&source=obs`。

### 9.9 设置页打开后音乐停止

正常情况下设置按钮会新开窗口，不应停止 OBS 播放。若浏览器拦截了弹窗，请直接访问：

```text
http://127.0.0.1:8000/order/settings.html?roomid=4646297
```

不要用设置页替代 OBS 播放页，也不要关闭唯一的 OBS 播放源。

## 10. 配置和文件安全

以下内容不要提交到 Git 或发给其他人：

- 网易云和 QQ Cookie；
- `data/credentials/`；
- `logs/`、`cache/`、`data/settings/` 中的运行信息；
- `config/config.yaml`、`config/webapi.js` 中的本地配置。

可检查 Git 是否忽略：

```bash
git status --short
git check-ignore -v config/config.yaml config/webapi.js src/public/webapi.js logs data cache
```

## 11. 相关开发文档

协议和实现细节位于 `doc/`：

- `doc/live_danmu_realtime_mode.md`：实时弹幕链路、指标和协议说明；
- `doc/get_live_damu.md`：B 站弹幕获取相关记录；
- `doc/get_lyric.md`：歌词和播放进度相关实现记录；
- `doc/insert_music.md`：歌曲插入和队列相关说明；
- `doc/bug.md`：问题记录。
